var VividSeats = (function() {
    var _events = [];

    return {
        eventService: {
            all: function(onSuccess, onError) {
                // See vivid ui project
            },
            add: function(event, onSuccess, onError) {
                // See vivid ui project
            }
        }
    }
})();

console.log(VividSeats);
